## 2026-04-30 - Native Form Validation
**Learning:** Native HTML5 validation (`required`) provides immediate, robust, and accessible validation feedback without the need for custom JS or styling. It correctly informs screen readers about mandatory fields.
**Action:** Always default to native HTML5 constraints for simple text inputs before considering custom validation logic.
